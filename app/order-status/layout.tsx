import type { Metadata } from "next";
export const metadata:Metadata={title:"Order Status | The Oddment Club",description:"Open your secure Shopify order status and delivery updates.",robots:{index:false,follow:false}};
export default function StatusLayout({children}:{children:React.ReactNode}){return children}
