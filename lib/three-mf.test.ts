import { afterEach,describe,expect,it } from "vitest";import { promises as fs } from "fs";import path from "path";import JSZip from "jszip";import { generate3mf } from "./three-mf";
const id="FF-TEST-MODEL";const file=path.join(process.cwd(),".data","models",`${id}.3mf`);afterEach(async()=>{await fs.unlink(file).catch(()=>{})});
describe("3MF generation",()=>{it("creates a valid two-object 3MF package with a true vector contour",async()=>{await generate3mf(id,{name:"MILO",font:"Block",baseColor:"#f4f0e7",topColor:"#1e1f22",icon:"flower",templateVersion:"1"});const zip=await JSZip.loadAsync(await fs.readFile(file));const xml=await zip.file("3D/3dmodel.model")!.async("string");expect(xml).toContain('unit="millimeter"');expect(xml).toContain('True 3.5 mm vector contour base');expect(xml).toContain('<object id="2"');expect(xml).toContain('<object id="3"');expect(xml).toContain('Base #f4f0e7');expect(zip.file("_rels/.rels")).not.toBeNull()})});
